repos:
  - repo: local
    hooks:
      - id: jekyll-rake-test
        name: jekyll build + HTMLProofer
        entry: bash -c 'cd "$(git rev-parse --show-toplevel)" && bundle exec rake test'
        language: system
        pass_filenames: false
        stages: [pre-push]
